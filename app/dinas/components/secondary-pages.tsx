"use client";

import { FormEvent, useState } from "react";
import { Bell, CheckCheck, ClipboardList, MapPin, Save, Settings, Trash2, Truck } from "lucide-react";
import { DlhShell } from "./dlh-shell";

const initialNotifications = [
  { id: 1, title: "Laporan prioritas tinggi", message: "Laporan #WL-099 di Menteng membutuhkan armada besar.", time: "8 menit lalu", type: "report", read: false },
  { id: 2, title: "Armada tiba di lokasi", message: "Truk Sampah 02 telah tiba untuk laporan #WL-098.", time: "18 menit lalu", type: "truck", read: false },
  { id: 3, title: "Laporan selesai", message: "Petugas menyelesaikan laporan #WL-096 di Cempaka Putih.", time: "1 jam lalu", type: "done", read: true },
  { id: 4, title: "Area kepadatan baru", message: "Sistem mendeteksi peningkatan laporan di Kecamatan Senen.", time: "2 jam lalu", type: "map", read: true },
];

export function NotificationsPage() {
  const [items, setItems] = useState(initialNotifications);
  const icon = (type: string) => type === "truck" ? <Truck className="size-5" /> : type === "map" ? <MapPin className="size-5" /> : type === "done" ? <CheckCheck className="size-5" /> : <ClipboardList className="size-5" />;

  return <DlhShell><main className="min-h-0 flex-1 overflow-y-auto bg-[#f5fbfe] p-4 sm:p-6 lg:p-8"><div className="mx-auto max-w-4xl"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#39815a]">Activity Center</p><h2 className="mt-1 text-2xl font-extrabold">Notifikasi</h2><p className="mt-1 text-sm text-slate-500">Pembaruan laporan dan operasional terkini.</p></div><button type="button" onClick={() => setItems((current) => current.map((item) => ({ ...item, read: true })))} className="flex items-center justify-center gap-2 rounded-full border border-[#bdd0c4] bg-white px-5 py-3 text-sm font-bold"><CheckCheck className="size-4" /> Tandai semua dibaca</button></div><div className="mt-6 overflow-hidden rounded-3xl border border-[#d8e7df] bg-white shadow-sm">{items.map((item) => <article key={item.id} onClick={() => setItems((current) => current.map((entry) => entry.id === item.id ? { ...entry, read: true } : entry))} className={`flex cursor-pointer gap-4 border-b border-slate-100 p-5 last:border-0 ${item.read ? "bg-white" : "bg-[#f0fbf5]"}`}><span className={`grid size-11 shrink-0 place-items-center rounded-2xl ${item.read ? "bg-slate-100 text-slate-500" : "bg-[#d5f1e1] text-[#087529]"}`}>{icon(item.type)}</span><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="font-extrabold">{item.title}</h3>{!item.read && <span className="size-2 rounded-full bg-[#087529]" />}</div><p className="mt-1 text-sm text-slate-600">{item.message}</p><p className="mt-2 text-xs text-slate-400">{item.time}</p></div><button type="button" onClick={(event) => { event.stopPropagation(); setItems((current) => current.filter((entry) => entry.id !== item.id)); }} aria-label={`Hapus ${item.title}`} className="self-center rounded-full p-2 text-slate-400 hover:bg-red-50 hover:text-red-600"><Trash2 className="size-4" /></button></article>)}{!items.length && <div className="py-16 text-center"><Bell className="mx-auto size-8 text-slate-300" /><p className="mt-3 text-sm text-slate-500">Tidak ada notifikasi.</p></div>}</div></div></main></DlhShell>;
}

export function SettingsPage() {
  const [saved, setSaved] = useState(false);
  const [autoDispatch, setAutoDispatch] = useState(true);
  const [emailAlert, setEmailAlert] = useState(true);
  const [soundAlert, setSoundAlert] = useState(false);

  const save = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setSaved(true); window.setTimeout(() => setSaved(false), 3000); };

  return <DlhShell><main className="min-h-0 flex-1 overflow-y-auto bg-[#f5fbfe] p-4 sm:p-6 lg:p-8"><form onSubmit={save} className="mx-auto max-w-4xl"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#39815a]">Portal Configuration</p><h2 className="mt-1 text-2xl font-extrabold">Pengaturan</h2><p className="mt-1 text-sm text-slate-500">Atur profil instansi dan preferensi operasional.</p></div>{saved && <div role="status" className="mt-5 rounded-2xl bg-[#def5e8] px-4 py-3 text-sm font-semibold text-[#166734]">Pengaturan berhasil disimpan.</div>}<section className="mt-6 rounded-3xl border border-[#d8e7df] bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#e5f6ed] text-[#087529]"><Settings className="size-5" /></span><div><h3 className="font-extrabold">Profil Instansi</h3><p className="text-xs text-slate-400">Informasi yang tampil pada portal DLH.</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="text-sm font-bold">Nama Instansi<input name="agency" defaultValue="Dinas Lingkungan Hidup" className="mt-2 h-11 w-full rounded-xl border px-4 font-normal outline-none focus:border-[#087529]" /></label><label className="text-sm font-bold">Wilayah<input name="region" defaultValue="Jakarta Pusat" className="mt-2 h-11 w-full rounded-xl border px-4 font-normal outline-none focus:border-[#087529]" /></label><label className="text-sm font-bold">Email Operasional<input name="email" type="email" defaultValue="operasional@dlh.go.id" className="mt-2 h-11 w-full rounded-xl border px-4 font-normal outline-none focus:border-[#087529]" /></label><label className="text-sm font-bold">Nomor Hotline<input name="phone" defaultValue="021-555-0199" className="mt-2 h-11 w-full rounded-xl border px-4 font-normal outline-none focus:border-[#087529]" /></label></div></section><section className="mt-5 rounded-3xl border border-[#d8e7df] bg-white p-6 shadow-sm"><h3 className="font-extrabold">Preferensi Sistem</h3><div className="mt-5 divide-y">{[{ label: "Rekomendasi dispatch otomatis", detail: "AI merekomendasikan armada dan petugas terdekat.", value: autoDispatch, set: setAutoDispatch }, { label: "Notifikasi melalui email", detail: "Kirim peringatan untuk laporan prioritas tinggi.", value: emailAlert, set: setEmailAlert }, { label: "Suara notifikasi", detail: "Putar suara saat laporan baru masuk.", value: soundAlert, set: setSoundAlert }].map((item) => <div key={item.label} className="flex items-center gap-4 py-4"><div className="flex-1"><p className="text-sm font-bold">{item.label}</p><p className="text-xs text-slate-400">{item.detail}</p></div><button type="button" onClick={() => item.set(!item.value)} aria-pressed={item.value} className={`relative h-7 w-12 rounded-full transition ${item.value ? "bg-[#087529]" : "bg-slate-300"}`}><span className={`absolute top-1 size-5 rounded-full bg-white shadow transition ${item.value ? "left-6" : "left-1"}`} /></button></div>)}</div></section><div className="sticky bottom-0 mt-5 flex justify-end border-t border-[#d8e7df] bg-[#f5fbfe]/95 py-4 backdrop-blur"><button className="flex items-center gap-2 rounded-full bg-[#087529] px-6 py-3 text-sm font-extrabold text-white"><Save className="size-4" /> Simpan Pengaturan</button></div></form></main></DlhShell>;
}
